services:
  embedding:
    build: .
    container_name: bge_embedding_service
    ports:
      - "8100:8100"
    environment:
      - MODEL_NAME=BAAI/bge-large-en-v1.5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
